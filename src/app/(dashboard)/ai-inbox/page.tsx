"use client";

import { useEffect, useState } from "react";
import { Loader2 } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { ClientSelect } from "@/components/shared/client-select";
import { Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle } from "@/components/ui/dialog";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";

type Intake = { id: string; filename: string | null; mime_type: string | null; status: string; error_message?: string | null; created_at: string; updated_at?: string };
type Draft = { id: string; intake_id: string | null; intake: Intake | null; title: string; description: string | null; type: string; client_id: string | null; maker_id: string | null; maker_name: string | null; due_at: string | null; confidence: number | null; clarification_needed: boolean; clarification_question: string | null; source_context?: string; status: string };
type Member = { profile_id: string; name: string; email: string | null; role: string };
export default function AiInboxPage() {
  const [file, setFile] = useState<File | null>(null); const [text, setText] = useState(""); const [drafts, setDrafts] = useState<Draft[]>([]); const [busy, setBusy] = useState(false); const [loading, setLoading] = useState(true); const [error, setError] = useState<string | null>(null); const [message, setMessage] = useState<string | null>(null); const [editing, setEditing] = useState<Draft | null>(null); const [form, setForm] = useState({ title: "", description: "", type: "ad_hoc", client_id: "", maker_id: "", due_at: "" }); const [members, setMembers] = useState<Member[]>([]); const [, setProcessingIntake] = useState<Intake | null>(null);
  const safeSourceContext = (value: string) => value.replace(/[\u0000-\u0008\u000B\u000C\u000E-\u001F\u007F]/g, " ").trim().slice(0, 1000);
  async function load() { setLoading(true); setError(null); try { const response = await fetch("/api/ai-intake"); const body = await response.json(); if (!response.ok) throw new Error(body.error ?? "Draft AI gagal dimuat."); setDrafts(body.data ?? []); } catch (err) { setError(err instanceof Error ? err.message : "Draft AI gagal dimuat."); } finally { setLoading(false); } }
  useEffect(() => { queueMicrotask(() => { void load(); }); }, []);
  async function waitForIntake(intakeId: string) { let latest: Intake | null = null; for (let attempt = 0; attempt < 30; attempt += 1) { const response = await fetch(`/api/ai-intake?intake_id=${encodeURIComponent(intakeId)}`); const body = await response.json(); if (!response.ok) throw new Error(body.error ?? "Gagal memeriksa status AI."); const intake = body.intake as Intake; latest = intake; if (intake.status === "draft" || intake.status === "failed") { setProcessingIntake(null); const intakeDrafts = (body.data ?? []).map((draft: Draft) => ({ ...draft, intake })); setDrafts((current) => [...intakeDrafts, ...current.filter((draft) => draft.intake_id !== intakeId)]); setMessage(intake.status === "failed" ? intake.error_message ?? "AI gagal memproses file." : intakeDrafts.length ? "AI selesai membuat draft." : "AI selesai membaca file, tetapi tidak menemukan action item yang eksplisit."); return; } setProcessingIntake(intake); await new Promise((resolve) => window.setTimeout(resolve, 2000)); } setProcessingIntake(null); setMessage(latest?.status === "queued" ? "File sudah masuk antrean, tetapi worker AI belum mengambilnya. Pastikan scheduler /api/jobs/ai-extraction aktif." : "File masih diproses oleh worker AI. Coba muat ulang beberapa saat lagi."); }
  async function submit() { setBusy(true); setMessage(null); const form = new FormData(); if (file) form.set("file", file); if (text.trim()) form.set("text", text); try { const response = await fetch("/api/ai-intake", { method: "POST", body: form }); const body = await response.json(); if (!response.ok) throw new Error(body.error ?? "Gagal memproses."); setText(""); setFile(null); await waitForIntake(body.data.intake_id); } catch (err) { setMessage(err instanceof Error ? err.message : "Gagal memproses."); } finally { setBusy(false); } }
  function openEditor(draft: Draft) { setEditing(draft); setForm({ title: draft.title, description: draft.description ?? draft.source_context ?? "", type: draft.type, client_id: draft.client_id ?? "", maker_id: draft.maker_id ?? "", due_at: draft.due_at ? draft.due_at.slice(0, 16) : "" }); }
  useEffect(() => { queueMicrotask(() => { if (!form.client_id) { setMembers([]); return; } void fetch(`/api/assignment-members?client_id=${encodeURIComponent(form.client_id)}`).then(async (response) => { const body = await response.json(); if (!response.ok) throw new Error(body.error ?? "Anggota gagal dimuat."); setMembers(body.data ?? []); }).catch((err) => setMessage(err instanceof Error ? err.message : "Anggota gagal dimuat.")); }); }, [form.client_id]);
  async function confirm() { if (!editing) return; setBusy(true); const response = await fetch(`/api/ai-intake/${editing.id}/confirm`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ ...form, maker_id: form.maker_id || null, due_at: form.due_at ? new Date(form.due_at).toISOString() : null }) }); const body = await response.json().catch(() => null); if (response.ok) { setEditing(null); setMessage("Task berhasil dibuat."); await load(); } else setMessage(body?.error ?? "Gagal konfirmasi."); setBusy(false); }
  const groups = Array.from(drafts.reduce((map, draft) => { const key = draft.intake_id ?? "ungrouped"; const group = map.get(key) ?? []; group.push(draft); map.set(key, group); return map; }, new Map<string, Draft[]>()).entries());
  function formatDateTime(value: string | null) { return value ? new Intl.DateTimeFormat("id-ID", { dateStyle: "medium", timeStyle: "short" }).format(new Date(value)) : "—"; }
  const acceptedFiles = ".txt,.csv,.json,.md,.log,.pdf,.docx,.xlsx,.pptx,text/*,application/pdf,application/vnd.openxmlformats-officedocument.wordprocessingml.document,application/vnd.openxmlformats-officedocument.spreadsheetml.sheet,application/vnd.openxmlformats-officedocument.presentationml.presentation";
  return <div className="mx-auto max-w-5xl space-y-6 px-4 py-6 sm:px-6"><div><h1 className="text-2xl font-semibold text-slate-900">Inbox AI</h1><p className="text-sm text-slate-500">Upload dokumen atau tempel teks. AI hanya membuat draft sebelum dikonfirmasi.</p></div><Card><CardHeader><CardTitle className="text-base">Input dokumen atau instruksi</CardTitle></CardHeader><CardContent className="space-y-3"><Label htmlFor="ai-file">File sumber</Label><input id="ai-file" type="file" accept={acceptedFiles} onChange={(event) => setFile(event.target.files?.[0] ?? null)} /><Label htmlFor="ai-text">Teks sumber</Label><textarea id="ai-text" aria-label="Teks sumber untuk AI" value={text} onChange={(event) => setText(event.currentTarget.value)} placeholder="Tempel isi dokumen atau instruksi pekerjaan..." rows={6} className="w-full rounded-lg border border-slate-200 p-3 text-sm" /><Button onClick={submit} disabled={busy || (!file && !text.trim())}>{busy ? "Memproses..." : "Baca dengan AI"}</Button>{message && <p role="status" className="text-sm text-slate-600">{message}</p>}</CardContent></Card><div className="space-y-3"><h2 className="text-lg font-semibold">Draft AI</h2>{loading ? <div className="flex items-center gap-2 text-sm text-slate-500"><Loader2 className="size-4 animate-spin" />Memuat draft...</div> : error ? <div className="space-y-2"><p role="alert" className="text-sm text-red-600">{error}</p><Button variant="outline" onClick={() => void load()}>Coba lagi</Button></div> : drafts.length === 0 ? <p className="text-sm text-slate-400">Belum ada draft.</p> : groups.map(([groupId, group]) => { const intake = group[0]?.intake; return <Card key={groupId}><CardHeader className="space-y-3"><div className="flex flex-col gap-2 sm:flex-row sm:items-start sm:justify-between"><div><CardTitle className="text-base">{intake?.filename ?? "Input teks langsung"}</CardTitle><p className="mt-1 text-xs text-slate-500">Batch ID: {intake?.id ?? "—"}</p></div><span className="w-fit rounded-full bg-slate-100 px-2.5 py-1 text-xs font-medium text-slate-700">{intake?.status ?? "Tanpa intake"}</span></div><div className="grid gap-2 text-xs text-slate-500 sm:grid-cols-3"><span>Upload: {formatDateTime(intake?.created_at ?? null)}</span><span>Jumlah draft: {group.length}</span><span>Status draft: {group.every((draft) => draft.status !== "draft") ? "Selesai" : "Perlu ditinjau"}</span></div></CardHeader><CardContent className="space-y-3">{group.map((draft) => <div key={draft.id} className="flex flex-col gap-3 rounded-lg border border-slate-200 p-4 sm:flex-row sm:items-center sm:justify-between"><div><p className="font-medium text-slate-900">{draft.title}</p><p className="text-xs text-slate-500">{draft.type} · Client: {draft.client_id ? "Sudah dipilih" : "Belum dipilih"} · PIC: {draft.maker_name ?? "Belum ditentukan"}</p>{draft.clarification_needed && <p className="mt-1 text-sm text-amber-700">{draft.clarification_question}</p>}</div>{draft.status === "draft" && <Button onClick={() => openEditor(draft)}>Edit & Konfirmasi</Button>}</div>)}</CardContent></Card>; })}</div><Dialog open={Boolean(editing)} onOpenChange={(open) => !open && setEditing(null)}><DialogContent className="max-w-2xl"><DialogHeader><DialogTitle>Edit draft sebelum membuat task</DialogTitle><DialogDescription>Periksa hasil AI. Pilih Client dan PIC dari daftar yang tersedia.</DialogDescription></DialogHeader><div className="grid gap-4 py-2"><label className="space-y-1.5"><Label htmlFor="ai-title">Judul</Label><Input id="ai-title" value={form.title} onChange={(event) => setForm({ ...form, title: event.currentTarget.value })} /></label><label className="space-y-1.5"><Label htmlFor="ai-description">Deskripsi</Label><textarea id="ai-description" value={form.description} onChange={(event) => setForm({ ...form, description: event.currentTarget.value })} rows={4} className="w-full rounded-lg border border-slate-200 p-3 text-sm" /></label><div className="grid gap-4 sm:grid-cols-2"><ClientSelect id="ai-draft-client" value={form.client_id} onChange={(value) => setForm({ ...form, client_id: value, maker_id: "" })} /><label className="space-y-1.5"><Label>Jenis pekerjaan</Label><Select value={form.type} onValueChange={(value) => setForm({ ...form, type: value ?? "ad_hoc" })}><SelectTrigger aria-label="Jenis pekerjaan"><SelectValue /></SelectTrigger><SelectContent><SelectItem value="routine">Routine</SelectItem><SelectItem value="project">Project</SelectItem><SelectItem value="ad_hoc">Ad Hoc</SelectItem><SelectItem value="report">Report</SelectItem></SelectContent></Select></label></div><label className="space-y-1.5"><Label>PIC / Maker</Label><Select value={form.maker_id || null} onValueChange={(value) => setForm({ ...form, maker_id: value ?? "" })}><SelectTrigger aria-label="PIC atau maker"><SelectValue placeholder={form.client_id ? "Pilih PIC" : "Pilih client dulu"} /></SelectTrigger><SelectContent>{members.map((member) => <SelectItem key={member.profile_id} value={member.profile_id}>{member.name}{member.email ? ` · ${member.email}` : ""}</SelectItem>)}</SelectContent></Select></label><label className="space-y-1.5"><Label htmlFor="ai-due">Tenggat</Label><Input id="ai-due" type="datetime-local" value={form.due_at} onChange={(event) => setForm({ ...form, due_at: event.currentTarget.value })} /></label>{editing?.source_context && <div className="rounded-lg bg-slate-50 p-3 text-xs text-slate-600">Konteks AI: {safeSourceContext(editing.source_context)}</div>}</div><DialogFooter><Button variant="outline" onClick={() => setEditing(null)}>Batal</Button><Button onClick={confirm} disabled={busy || !form.title.trim() || !form.client_id}>{busy ? "Membuat..." : "Konfirmasi & Buat Task"}</Button></DialogFooter></DialogContent></Dialog></div>;
}
